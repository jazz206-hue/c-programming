#include <stdio.h>

void main() 
{
    float a,b,c,d,e,f,g,h;
    printf("Enter any amount ");
    scanf("%f", &a);
    b=a/10;
    c=a/20;
    d=a/50;
    e=a/100;
    f=a/200;
    g=a/1000;
    printf("enter the number of notes of certain amout");
scanf("%f",h);
    if(h==10)
{
printf("the noof notes are %f",b);
}
else if(h==10)
{
printf("the noof notes are %f",b);
}
else if(h==20)
{
printf("the noof notes are %f",c);
}
else if(h==50)
{
printf("the noof notes are %f",d);
}
else if(h==100)
{
printf("the noof notes are %f",e);
}
else if(h==200)
{
printf("the noof notes are %f",f);
}
else if(h==1000)
{
printf("the noof notes are %f",g);
}
}
    